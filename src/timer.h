/*
2 minute timer code
Need to define a button
and an active buzzer pin.  

*/

void timerCheck(time_t localTime_t)
{
    time_t timerStart;
    time_t timerEnd;
    if (timerStart == timerEnd)
    {//make buzzerpin high
    }
}